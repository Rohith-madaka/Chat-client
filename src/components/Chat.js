import React, { Component } from 'react'

export default class chat extends Component {
    render() {
        return (
            <div>
                <input/>
                <button>send</button>
            </div>
        )
    }
}
